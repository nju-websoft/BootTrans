- role: user
  content: |
    Please translate source ${src_lang} code to ${tgt_lang} code:
    ```${src_lang}
    ${src_code}
    ```
    The translated ${tgt_lang} code should be:
    ```${tgt_lang}
    ${prompt}
    ```